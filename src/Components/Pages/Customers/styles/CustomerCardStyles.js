import { makeStyles } from '@material-ui/core/styles';

const CustomerCardStyles = makeStyles((theme) => ({
	root: {
		minWidth: 500
	},
	green: {
		backgroundColor: 'rgba(165, 214, 167,1)'
	}
}));

export default CustomerCardStyles;
